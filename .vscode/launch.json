{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node", 
            "request": "launch", 
            "name": "Debug Jasmine Tests", 
            "program": "${workspaceFolder}/node_modules/jasmine/bin/jasmine",
            "args": ["--reporter=jasmine-console-reporter", "--config=jasmine.json"],
            "console": "integratedTerminal"
        }
    ]
}